version: '3'

services:

  noon:
    build:
      context: .
    ports2222:22'
      - '7272:22'
    volumes:
      - /retek_mule:/data

  production_invoices:
    build:
      context: .
      dockerfile: sftp.dockerfile
    ports:
      - '7373:22'
    volumes:
      - /sftp/production_invoices:/home
